Friend MustInherit Class FeatureSubtypes
    Private Sub New()
    End Sub
    Friend Const CACTUS As String = NameOf(CACTUS)
    Friend Const CHECKPOINT As String = NameOf(CHECKPOINT)
    Friend Const DOOR As String = NameOf(DOOR)
    Friend Const GRAVESTONE As String = NameOf(GRAVESTONE)
    Friend Const SPAWNER As String = NameOf(SPAWNER)
End Class
